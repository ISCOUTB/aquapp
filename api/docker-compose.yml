version: '2.1'

services:
  api:
    build: ./
    ports:
      - 8080:3000
    depends_on:
      - aquappdb

  aquappdb:
    image: 'mongo:4.0.5'
    ports:
      - 27016:27017
    volumes:
      - ./aquappndb:/data/db
      - ./001_users.js:/docker-entrypoint-initdb.d/001_users.js:ro
    restart: always
    env_file:
      - ./mongo.env
